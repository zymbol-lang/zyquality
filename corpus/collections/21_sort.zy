// Test: $^+ (sort ascending) and $^- (sort descending) — natural order only
// Covers: integers, strings, floats, single element, empty array.

// ── Integers — natural order ───────────────────────────────────────────────
nums = [3, 1, 4, 1, 5, 9, 2, 6]
asc = nums$^+
desc = nums$^- 
>> asc[1] ¶
>> asc[8] ¶
>> desc[1] ¶
>> desc[8] ¶

// ── Strings — lexicographic ────────────────────────────────────────────────
words = ["banana", "apple", "cherry", "date"]
wa = words$^+
wd = words$^-
>> wa[1] ¶
>> wa[4] ¶
>> wd[1] ¶
>> wd[4] ¶

// ── Original array is unchanged ────────────────────────────────────────────
>> nums[1] ¶

// ── Floats ─────────────────────────────────────────────────────────────────
flt = [3.14, 1.41, 2.71, 0.57]
fa = flt$^+
fd = flt$^-
>> fa[1] ¶
>> fd[1] ¶

// ── Single element — no-op ─────────────────────────────────────────────────
one = [42]
>> (one$^+)[1] ¶
>> (one$^-)[1] ¶

// ── Empty array — no-op ────────────────────────────────────────────────────
empty = []
ea = empty$^+
>> ea$# ¶
