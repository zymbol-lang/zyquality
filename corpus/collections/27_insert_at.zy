// Test: $+[i] — insert element at position (1-based indexing)
// Result is a new array; original is unchanged.

// ── Insert in the middle (at 1-based position 3) ───────────────────────────
arr = [10, 20, 30, 40]
arr2 = arr$+[3] 99
>> arr2[1] ¶          // → 10
>> arr2[2] ¶          // → 20
>> arr2[3] ¶          // → 99  (inserted)
>> arr2[4] ¶          // → 30
>> arr2[5] ¶          // → 40
>> arr2$# ¶           // → 5

// ── Insert at beginning (index 1) ──────────────────────────────────────────
arr3 = arr$+[1] 0
>> arr3[1] ¶          // → 0
>> arr3[2] ¶          // → 10
>> arr3$# ¶           // → 5

// ── Insert at end (index = length + 1) ─────────────────────────────────────
arr4 = arr$+[5] 50
>> arr4[-1] ¶         // → 50
>> arr4$# ¶           // → 5

// ── Original unchanged ──────────────────────────────────────────────────────
>> arr$# ¶            // → 4
>> arr[1] ¶           // → 10
