// Test: Complex module with blocks and scopes

<# ./complex_module => complex

>> "=== Test 1: Module function with IF blocks ===" ¶

result1 = complex::process_with_blocks(15)
>> "Result: " result1 ¶

¶

result2 = complex::process_with_blocks(7)
>> "Result: " result2 ¶

¶

result3 = complex::process_with_blocks(3)
>> "Result: " result3 ¶

¶

>> "=== Test 2: Module function with loop and blocks ===" ¶

numbers = [5, -2, 10, -1, 3, 0, 7]
filtered = complex::filter_values(numbers)

>> "Filtered array: " filtered ¶
>> "Length: " filtered$# ¶

¶

>> "=== Test 3: Nested usage in main program blocks ===" ¶

? #1 {
    local_value = 12
    block_result = complex::process_with_blocks(local_value)
    >> "Block result: " block_result ¶
}

// local_value is destroyed here
// >> local_value ¶  // Would error

¶

>> "=== All complex module tests passed! ===" ¶
