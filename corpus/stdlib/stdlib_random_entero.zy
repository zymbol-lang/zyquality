// Test: random::entero(min, max) always returns Int in [min, max]
<# std/random => rnd

ok = #1
i = 0
@ (i < 100) {
    n = rnd::entero(1, 6)
    ? (n < 1 || n > 6) { ok = #0 }
    i = i + 1
}
? ok { >> "ok" ¶ }
? (!ok) { >> "fail" ¶ }
