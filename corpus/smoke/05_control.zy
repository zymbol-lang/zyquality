// if / else-if / else
x = 7
? x > 0 { >> "positive" ¶ }
? x > 100 {
    >> "large" ¶
} _? x > 0 {
    >> "small positive" ¶
} _? x == 0 {
    >> "zero" ¶
} _ {
    >> "negative" ¶
}
? x < 0 { >> "neg" ¶ } _ { >> "nonneg" ¶ }
y = 0
? y == 0 {
    ? x == 7 { >> "nested ok" ¶ }
}
