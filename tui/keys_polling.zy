>>| {
    >>!
    n = 0
    @ {
        <<|? k
        ? k <> '\0' {
            ? k == 'q' { @! }
            >>~ (1, 1) > "key=" k " n=" n
            n += 1
        }
        @~ 20
        ? n > 3 { @! }
    }
    >> "done n=" n "|"
}
