// @reject: a float is not a count — `@ 3.5` cannot mean "three and a half
// @reject: times" — and it is not a condition either. The VM used to accept it
// @reject: as a count and run the body 4 times, which is a rounding rule nobody
// @reject: wrote down.
n = 3.5
@ n {
    >> "x" ¶
}
