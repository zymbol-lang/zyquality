// @reject: una edición necesita un sitio con NOMBRE donde escribir. `f()[1]$~ 5`
// @reject: modifica lo que devolvió la llamada, y a eso no lo sujeta nadie: la
// @reject: siguiente línea lo descarta, así que la edición es inobservable por
// @reject: construcción.
// @reject:
// @reject: Es la decisión 20, que el comentario de `in_place_edit_target` llevaba
// @reject: prometiendo desde que se escribió —«is refused rather than silently
// @reject: doing nothing»— sin que nada la aplicara: la sentencia caía a
// @reject: `Statement::Expr`, se evaluaba y tiraba el resultado. El motor del
// @reject: navegador SÍ la rechazaba, así que era además una divergencia viva.
// @reject:
// @reject: Un receptor DENTRO de un nombre sí vale, y escribe donde toca:
// @reject: `d.x["y"]$~ 5` está en corpus/collections/escritura_por_punto.zy.
f() { <~ [1, 2] }
f()[1] $~ 5
>> f() ¶
